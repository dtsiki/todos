import { createStore } from 'redux';import rootReducers from './reducers';import { loadState, saveState } from '../services'const initialState = {  todos: [{completed: false, text: "Do something"}, {completed: true, text: "Do something else"}]};const preloadedState = {todosReducer: localStorage.length !== 0 ? loadState() : initialState};export const store = createStore(rootReducers, preloadedState);store.subscribe(() => {  saveState({    todos: store.getState().todosReducer.todos  })})